import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    fontFamily: "'Noto Sans', sans-serif",
  },
  palette: {
    primary: {
      main: "#378B05",
    },
    secondary: {
      main: "#26580F",
    },
    background: {
      default: "#eff8e6",
    },
  },
});
